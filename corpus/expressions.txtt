====
Expressions test
====

label:
label:  abx     ; comment
        abx     ; comment
        abx

---

(source_file
    (label)
    (label) (opcode (memnonic)) (semicolon_comment)
    (opcode (memnonic)) (semicolon_comment)
    (opcode (memnonic))
)
